import { connect } from 'react-redux'

import Profile from '../components/userPage/profile/Profile.jsx'

const mapStateToProps = () => ({

})

const mapDispatchToProps = () => ({

})

const PersonalizedProfile = connect(
  mapStateToProps,
  mapDispatchToProps
)(Profile)

export default PersonalizedProfile